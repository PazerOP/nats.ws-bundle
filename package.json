{
  "name": "nats.ws-bundle",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild node_modules/nats.ws/esm/nats.js --bundle --format=iife --global-name=nats --platform=browser --external:crypto --outfile=dist/nats.bundle.js"
  },
  "dependencies": {
    "nats.ws": "^1.30.3"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
